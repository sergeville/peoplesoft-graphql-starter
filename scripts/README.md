# Repo scripts

Runnable helpers in this folder. Each file header and console output points at the matching **course module**; course text links back here.

**Two-way index:** [Courses/SCRIPT_COURSE_LINKS.md](../Courses/SCRIPT_COURSE_LINKS.md) — **By course module** (course → script) and **npm commands** (script → course).

| File | npm command |
|------|-------------|
| `stop-dev-stack.sh` | `npm run stack:stop` |
| `setup-git-hooks.mjs` | `npm run prepare` (on install) |
| `bump-patch-version.mjs` | `npm run version:patch` / pre-commit |
| `install-apollo-mcp.sh` | `npm run mcp:install` (Section 13) |
| `run-apollo-mcp.sh` | `npm run dev:mcp` (Section 13) |

Backend data scripts: `backend/scripts/` — see the same index.  
Apollo MCP config: `apollo-mcp/` — [MODULE_13](../Courses/MODULE_13_APOLLO_MCP_AGENTS.md).
